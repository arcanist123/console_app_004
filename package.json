{
  "name": "console_app_004",
  "version": "1.0.1",
  "main": "index.js",
  "scripts": {
    "rename": "node scripts/rename_to_xsjs.js",
    "build": "rimraf tgt && tsc && rimraf xsjs && babel tgt -d xsjs && npm run rename",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@babel/cli": "^7.26.4",
    "@babel/core": "^7.26.9",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.3"
  }
}
